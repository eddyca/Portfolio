$(function(){
  
  var $container = $('#container'),
      $body = $('body'),
      colW = 280,
      columns = null;
  
  $container.isotope({
    // disable window resizing
    resizable: false,
    masonry: {
      columnWidth: colW
    }
  });
  

  
});
